#include <stdio.h>
#include <stdlib.h>
#define N 5

int main(int argc, char const *argv[])
{
    /* code */
    int a[N] , i ;
    for (i = 0 ; i <  N ; i++ )
    {
        printf("enter elements");
        scanf("%d" ,&a[i]);  
    }
    printf("enter element to be inserted at end");
    scanf("%d" ,&a[i]);  
    return 0;
}
